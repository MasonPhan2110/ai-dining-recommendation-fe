export type Restaurant = {
  id: string;
  name: string;
  cuisine: string;
  priceRange: string;
  distanceKm: number;
  tags: string[];
  matchScore: number;
  explanation: string;
  rating: number;
  address: string;
};
